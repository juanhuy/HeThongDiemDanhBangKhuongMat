# Mẫu cấu hình môi trường — copy sang .env rồi điền giá trị thật.
# Giá trị ở đây chỉ là ví dụ, KHÔNG dùng cho production.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=ptit_diem_danh
DB_ENCRYPTION_KEY=change_me_fernet_key
JWT_SECRET_KEY=change_me_jwt_secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480
